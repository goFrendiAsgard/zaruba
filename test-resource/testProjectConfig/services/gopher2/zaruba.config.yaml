env:
  GOPHER2_HTTP_PORT: 3012

components:
  gopher2:
    type: service
    origin: "" # fill this one with your git repository
    branch: master
    location: "."
    start: go build && ./app
    readinessCheck: curl ${gopher2}:${GOPHER1_HTTP_PORT}/readiness
    dependencies:
      - rmq

links:
  ../../libraries/go/transport:
    - ./transport
