package config

// GitIgnoreSeparator separate user's gitignore items with zaruba's
const GitIgnoreSeparator string = "# zaruba's"

// TemplateConfigFile contains template configuration
const TemplateConfigFile string = "zaruba.template.yaml"

// IgnoreFile contains regex pattern of files that should be ignored by zaruba
const IgnoreFile string = "zaruba.ignore"

// HookFile contains zaruba's hook
const HookFile string = "zaruba.hook.yaml"
