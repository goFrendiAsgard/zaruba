tasks:

  makeHelmTask:
    extend: makeTask
    inputs:
      - appDirectory
      - appHelmChartUrl
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      templateLocations: '["{{ .GetPath "../../templates/helmTask" }}"]'