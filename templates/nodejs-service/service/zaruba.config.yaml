environments:
  general:
    SERVICENAME_HTTP_PORT: 3010
    SERVICENAME_EVENT: servicename
  services: {}

components:
  servicename:
    type: service
    origin: "" # fill this one with your git repository
    branch: master
    location: "."
    start: npm install && npm start

links:
  ../../libraries/nodejs/helpers/amqp:
    - ./helpers/amqp
  ../../libraries/nodejs/helpers/express: 
    - ./helpers/express
