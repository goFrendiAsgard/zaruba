environments:
  general:
    SERVICENAME_HTTP_PORT: 3010
    SERVICENAME_EVENT: servicename
  services: {}

components:
  servicename:
    type: service
    origin: "" # fill this one with your git repository
    branch: master
    location: "."
    start: npm start
    readinessCheck: curl 0.0.0.0:${SERVICENAME_HTTP_PORT}/readiness

links:
  ../../libraries/typescript/transport:
    - ./src/transport
