environments:
  general:
    SERVICENAME_HTTP_PORT: 3010
  services: {}

components:
  servicename:
    type: service
    origin: "" # fill this one with your git repository
    branch: master
    location: "."
    start: npm install && npm start

