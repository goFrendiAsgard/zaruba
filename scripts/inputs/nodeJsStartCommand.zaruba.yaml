inputs:

  nodeJsStartCommand:
    description: Command to start the service (Required)
    prompt: Start command
    options:
      - npm start
      - node start
      - node main
      - node index
    default: npm start
    validation: ^.+$

