inputs:

  servicePorts:
    description: |
      Service ports JSON formated.
      E.g: ["3001:3000", "8080" , "{{ .GetEnv \"HTTP_PORT\" }}"]
    prompt: "Service ports, JSON formated. E.g: [\"3001:3000\", \"8080\", \"{{ .GetEnv \\\"HTTP_PORT\\\"]"
    validation: ^\[.*\]$
    default: "[]"