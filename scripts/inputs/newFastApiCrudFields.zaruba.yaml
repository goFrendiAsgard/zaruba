inputs:
  
  newFastApiCrudFields:
    description: |
      Field names, JSON formated.
      E.g: ["name", "address"]
    prompt: "Field names, JSON formated. E.g: [\"name\", \"address\"]"
    validation: ^\[.*\]$
    default: "[]"
 