tasks:

  makeTerraformTask:
    extend: makeTask
    inputs:
      - templateLocation
      - appDirectory
    config:
      templateLocation: '{{ .GetRelativePath "../../templates/terraformTask" }}'
