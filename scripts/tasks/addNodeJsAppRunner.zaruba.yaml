tasks:

  addNodeJsAppRunner:
    extend: makeRunnerTask
    dependencies:
      - zrbMakeZrbApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      _prepareVariables: |
        . "{{ .GetRelativePath "../../bash/prepareDefaultMakeVariables.sh" }}"
        . "{{ .GetRelativePath "../../bash/prepareNodeJsTaskGenVariables.sh" }}"
      defaultAppPorts: |
        [
          "3000"
        ]
      templateLocations: |
        [
          "{{ .GetRelativePath "../../templates/runnerTask/base" }}",
          "{{ .GetRelativePath "../../templates/runnerTask/nodejs" }}"
        ]
  