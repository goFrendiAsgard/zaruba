tasks:

  addGoAppRunner:
    extend: makeRunnerTask
    dependencies:
      - zrbMakeZrbApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      _prepareVariables: |
        . "{{ .GetPath "../../bash/prepareDefaultMakeVariables.sh" }}"
        . "{{ .GetPath "../../bash/prepareGoTaskGenVariables.sh" }}"
      defaultAppPorts: |
        [
          "3000"
        ]
      templateLocations: |
        [
          "{{ .GetPath "../../templates/runnerTask/base" }}",
          "{{ .GetPath "../../templates/runnerTask/go" }}"
        ]
  