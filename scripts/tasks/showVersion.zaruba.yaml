tasks:

  showVersion:
    icon: 🔎
    description: |
      Show zaruba's current version.
    extend: core.runCoreScript
    config:
      start: |
        {{ $d := .Decoration -}}
        cd "${ZARUBA_HOME}"
        echo "{{ $d.Bold }}{{ $d.Yellow }}$(getVersion){{ $d.Normal }}"