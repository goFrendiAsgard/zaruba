tasks:

  makeRunnerTask:
    extend: makeTask
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      templateLocation: '{{ .GetRelativePath "../../templates/runnerTask" }}'