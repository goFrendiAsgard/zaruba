tasks: 

  makeNginxDockerTask:
    extend: core.makeDockerTask
    inputs:
      - dockerContainerName
      - serviceName
      - serviceEnvs
      - taskDependencies
    config:
      templateLocation: '{{ .GetEnv "ZARUBA_HOME" }}/templates/task/docker/nginx'
    

