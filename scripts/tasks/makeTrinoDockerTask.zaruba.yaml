tasks: 

  makeTrinoDockerTask:
    extend: core.makeDockerTask
    inputs:
      - generatorDockerContainerName
      - generatorServiceName
      - generatorServiceEnvs
      - generatorTaskDependencies
    config:
      templateLocation: '{{ .GetEnv "ZARUBA_HOME" }}/templates/task/docker/trino'

