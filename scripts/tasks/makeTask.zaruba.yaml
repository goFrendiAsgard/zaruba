tasks:

  makeTask:
    extend: make
    private: true
    config:
      _integrateModule: '. "{{ .GetRelativePath "../../bash/registerModule.sh" }}" "${_ZRB_PROJECT_FILE_NAME}" "${_ZRB_MODULE_FILE_NAME}" "${_ZRB_APP_NAME}"'
      _integrateModuleTasks: '. "{{ .GetRelativePath "../../bash/registerModuleTask.sh" }}" "${_ZRB_PROJECT_FILE_NAME}" "${_ZRB_MODULE_FILE_NAME}" "${_ZRB_APP_NAME}"'
      _integrate: |
        {{ .GetConfig "_integrateModule" }}
        {{ .GetConfig "_integrateModuleTasks" }}
