tasks:
 
  zrbMakeMysqlApp:
    extend: makeApp
    private: true
    inputs:
      - appDirectory
    config:
      templateLocations: |
        [
          "{{ .GetPath "../../templates/app/mysql" }}"
        ]

  addMysql:
    extend: makeRunnerTask
    dependencies:
      - zrbMakeMysqlApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      defaultAppContainerVolumes: |
        [
          "./init.sql:/docker-entrypoint-initdb.d/init.sql"
        ]
      defaultAppPorts: |
        [
          "3306"
        ]
      templateLocations: |
        [
          "{{ .GetPath "../../templates/runnerTask/base" }}",
          "{{ .GetPath "../../templates/runnerTask/container" }}"
        ]
  