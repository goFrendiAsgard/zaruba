tasks:

  runService:
    icon: 🐍
    extend: core.startService
    config:
      script: |
        pipenv install
        pipenv run uvicorn main:app --reload --port={{ .GetEnv "HTTP_PORT" }}
    env:
      PIPENV_DONT_LOAD_ENV:
        from: PIPENV_DONT_LOAD_ENV
        default: 1
      HTTP_PORT:
        from: HTTP_PORT
        default: 3000
  