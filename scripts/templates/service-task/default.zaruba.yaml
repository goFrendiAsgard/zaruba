tasks:

  runService:
    icon: 📜
    description: Serve static files
    extend: serveHttp
    env:
      HTTP_PORT:
        from: HTTP_PORT
        default: 8080
    lconfig:
      ports:
      - '{{ .GetEnv "HTTP_PORT" }}'