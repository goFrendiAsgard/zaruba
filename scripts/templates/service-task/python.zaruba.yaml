tasks:

  runService:
    icon: 🐍
    extend: core.startService
    config:
      start: |
        pipenv install
        MAIN_SCRIPT=$(sh "${UTIL}/get_python_main_script.sh")
        pipenv run python ${MAIN_SCRIPT}
    env:
      PIPENV_DONT_LOAD_ENV:
        from: PIPENV_DONT_LOAD_ENV
        default: 1
  