inputs:
  
  setupAllowRoot:
    default: no
    options: [yes, no]
    allowCustom: no
    description: |
      Allow to install as root (using root home directory)
    prompt: Allow to install as root
  

