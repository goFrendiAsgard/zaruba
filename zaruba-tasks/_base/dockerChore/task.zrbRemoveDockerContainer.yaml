tasks:

  zrbRemoveDockerContainer:
    icon: 🐳
    private: true
    description: |
      Remove docker container.
      Common configs:
        containerName : Container's name
    extend: zrbRunShellScript
    dependencies:
      - updateProjectLinks
    configRef: zrbRemoveDockerContainer