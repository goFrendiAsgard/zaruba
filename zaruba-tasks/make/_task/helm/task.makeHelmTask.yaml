tasks:

  makeHelmTask:
    extend: zrbMakeTask
    inputs:
      - appDirectory
      - appHelmDirectory
      - appHelmReleaseName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      templateLocations: '["{{ .ZarubaHome }}/make/_task/helm/template"]'