tasks:

  makePythonAppRunner:
    icon: 🐍
    extend: makeNativeAppRunner
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appStartCommand
      - appTestCommand
      - appMigrateCommand
      - appCheckCommand
      - appImageName
      - appContainerName
    configRefs:
      - makePythonAppRunner
      - addPythonAppRunner