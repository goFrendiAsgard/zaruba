tasks:

  makePythonAppRunner:
    icon: 🐍
    extend: makeNativeAppRunner
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRefs:
      - makePythonAppRunner
      - addPythonAppRunner