tasks:

  makePythonAppRunner:
    icon: 🐍
    extend: makeNativeAppRunner
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appPorts
      - appStartCommand
      - appTestCommand
      - appMigrateCommand
      - appCheckCommand
      - appImageName
      - appContainerName
    configRefs:
      - makePythonAppRunner