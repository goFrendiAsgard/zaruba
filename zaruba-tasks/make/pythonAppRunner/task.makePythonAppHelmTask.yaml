tasks:

  makePythonAppHelmTask:
    icon: 🐍
    extend: zrbMakeHelmTask
    dependencies:
      - makePythonAppHelm
      - makePythonAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRef: addPythonAppRunner