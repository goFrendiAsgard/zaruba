tasks:

  makeGoAppHelmTask:
    icon: 🐹
    extend: zrbMakeHelmTask
    dependencies:
      - makeGoAppHelm
      - makeGoAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRef: addGoAppRunner