tasks:

  makeMeltanoAppRunner:
    icon: 🐉
    extend: makeNativeAppRunner
    dependencies:
      - makeMeltanoApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appImageName
      - appContainerName
    configRef: makeMeltanoAppRunner