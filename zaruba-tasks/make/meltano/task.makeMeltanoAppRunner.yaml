tasks:

  makeMeltanoAppRunner:
    icon: 🐉
    extend: makeNativeAppRunner
    dependencies:
      - makeMeltanoApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appImageName
      - appContainerName
    configRef: makeMeltanoAppRunner