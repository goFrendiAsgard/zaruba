tasks:

  makeRabbitmqApp:
    icon: 🐇
    extend: makeApp
    inputs:
      - appDirectory
    configs:
      defaultAppDirectory: '{{ .ProjectName }}Rabbitmq'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/rabbitmq/appTemplate"
        ]