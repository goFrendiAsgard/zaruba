tasks:

  makeGitlabAppRunner:
    icon: 🦊
    extend: makeDockerContainerAppRunner
    dependencies:
      - makeGitlabApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeGitlabAppRunner