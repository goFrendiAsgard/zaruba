tasks:

  makeGitlabAppRunner:
    icon: 🦊
    extend: makeDockerComposeAppRunner
    dependencies:
      - makeGitlabApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appDependencies
      - appContainerName
    configRef: makeGitlabAppRunner