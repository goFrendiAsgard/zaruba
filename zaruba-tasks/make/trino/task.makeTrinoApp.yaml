tasks:

  makeTrinoApp:
    icon: 🐰
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Trino'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/trino/appTemplate"
        ]