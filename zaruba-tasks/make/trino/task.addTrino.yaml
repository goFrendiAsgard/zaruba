tasks:
 
  makeTrinoApp:
    icon: 🐰
    extend: makeApp
    inputs:
      - appDirectory
    config:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/trino/template"
        ]

  addTrino:
    icon: 🐰
    extend: makeContainerAppRunner
    dependencies:
      - makeTrinoApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      appIcon: 🐰
      defaultAppPorts: |
        [
          "8080"
        ]
      defaultAppContainerVolumes: |
        [
          "trino:/etc/trino"
        ]