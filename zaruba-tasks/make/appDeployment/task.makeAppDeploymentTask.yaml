tasks:

  makeAppDeploymentTask:
    icon: 🚢
    extend: zrbMakeTask
    dependencies:
      - makeAppDeployment
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppDeploymentTask

