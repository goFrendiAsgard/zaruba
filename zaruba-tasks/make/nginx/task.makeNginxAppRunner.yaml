tasks:

  makeNginxAppRunner:
    icon: 📗
    extend: makeDockerAppRunner
    dependencies:
      - makeNginxApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeNginxAppRunner