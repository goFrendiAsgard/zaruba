tasks:

  makeNginxAppRunner:
    icon: 📗
    extend: makeDockerContainerAppRunner
    dependencies:
      - makeNginxApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeNginxAppRunner