tasks:
 
  makeNginxApp:
    icon: 📗
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Nginx'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/nginx/appTemplate"
        ]