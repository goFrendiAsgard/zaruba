tasks:
 
  makeKafkaApp:
    icon: 🪠
    extend: makeApp
    inputs:
      - appDirectory
    configs:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/kafka/appTemplate"
        ]