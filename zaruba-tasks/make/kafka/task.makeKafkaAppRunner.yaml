tasks:

  makeKafkaAppRunner:
    icon: 🚌
    extend: makeDockerContainerAppRunner
    dependencies:
      - makeKafkaApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - kafkaPorts
      - zookeeperPorts
      - kafkaSchemaRegistryPorts
      - kafkaConnectPorts
      - akhqPorts
      - appImageName
      - appContainerName
    configRef: makeKafkaAppRunner