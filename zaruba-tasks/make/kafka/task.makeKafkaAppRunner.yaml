tasks:

  makeKafkaAppRunner:
    icon: 🚌
    extend: makeDockerComposeAppRunner
    dependencies:
      - makeKafkaApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appDependencies
      - appContainerName
    configRef: makeKafkaAppRunner