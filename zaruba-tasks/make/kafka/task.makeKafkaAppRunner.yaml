tasks:

  makeKafkaAppRunner:
    icon: 🪠
    extend: makeDockerAppRunner
    dependencies:
      - makeKafkaApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - kafkaPorts
      - zookeeperPorts
      - appImageName
      - appContainerName
    configRef: makeKafkaAppRunner