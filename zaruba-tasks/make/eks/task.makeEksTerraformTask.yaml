tasks:

  makeEksTerraformTask:
    icon: 📙
    extend:  zrbMakeTerraformTask
    inputs:
      - appDirectory
    dependencies:
      - makeEksApp
    configRef: addEks
    config:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/_task/terraform/template",
          "{{ .ZarubaHome }}/zaruba-tasks/make/eks/terraformTaskTemplate"
        ]