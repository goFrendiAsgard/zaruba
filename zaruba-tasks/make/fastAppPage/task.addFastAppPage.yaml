tasks:
 
  addFastAppPage:
    icon: ⚛️
    extend: makeApp
    dependencies:
      - addFastAppModule
    inputs:
      - appDirectory
      - appModuleName
      - appUrl
    configRef: addFastAppPage