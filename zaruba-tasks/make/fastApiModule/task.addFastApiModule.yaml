tasks:
 
  addFastApiModule:
    icon: ⚡
    extend: makeApp
    dependencies:
      - addFastApi
    inputs:
      - appDirectory
      - appModuleName
    configs:
      skipCreationPath: '${_ZRB_APP_DIRECTORY}/${_ZRB_APP_MODULE_NAME}'
      _integrate: |
        . "{{ .ZarubaHome }}/zaruba-tasks/make/fastApiModule/bash/registerModule.sh"
      defaultAppDirectory: '{{ .ProjectName }}FastApi'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/fastApiModule/template"
        ]