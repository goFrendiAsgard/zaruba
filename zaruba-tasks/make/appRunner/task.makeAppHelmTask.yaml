tasks:

  makeAppHelmTask:
    icon: 🐶
    extend: zrbMakeHelmTask
    dependencies:
      - makeAppHelm
      - makeAppRunner
    inputs:
      - appDirectory
      - appName
      - appHelmDirectory
      - appHelmReleaseName
    configRef: addAppRunner