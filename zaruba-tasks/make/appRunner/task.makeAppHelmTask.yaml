tasks:

  makeAppHelmTask:
    icon: 🐶
    extend: zrbMakeHelmTask
    dependencies:
      - makeAppHelm
      - makeAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRef: addAppRunner