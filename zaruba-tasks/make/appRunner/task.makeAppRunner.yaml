tasks:

  makeAppRunner:
    icon: 🐶
    extend: makeNativeAppRunner
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
      - appStartCommand
    configRef: makeAppRunner