tasks:

  makeAppRunner:
    icon: 🐶
    extend: makeNativeAppRunner
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
      - appStartCommand
    configRef: makeAppRunner