name: ztplDeploymentName
runtime:
  name: python
  options:
    virtualenv: venv
description: Deploying EC2 and RDS
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-2

