tasks:
 
  makeFastApiApp:
    icon: ⚡
    extend: makeApp
    inputs:
      - appDirectory
    configs:
      defaultAppDirectory: '{{ .ProjectName }}FastApi'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/fastApi/appTemplate"
        ]