tasks:

  makeFastApiAppRunner:
    icon: ⚡
    extend: addPythonAppRunner
    dependencies:
      - makeFastApiApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: addFastApi