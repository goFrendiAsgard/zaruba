tasks:

  makeFastApiHelmTask:
    icon: ⚡
    extend: zrbMakeHelmTask
    dependencies:
      - makeFastApiHelm
      - makeFastApiAppRunner
    inputs:
      - appDirectory
      - appName
      - appHelmDirectory
      - appHelmReleaseName
    configRef: addFastApi