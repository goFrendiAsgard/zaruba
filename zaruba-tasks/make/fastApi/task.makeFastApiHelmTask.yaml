tasks:

  makeFastApiHelmTask:
    icon: ⚡
    extend: zrbMakeHelmTask
    dependencies:
      - makeFastApiHelm
      - makeFastApiAppRunner
    inputs:
      - appDirectory
      - appName
      - appHelmDirectory
      - appHelmReleaseName
    config:
      appIcon: ⚡
      defaultAppDirectory: '{{ .ProjectName }}FastApiHelm'