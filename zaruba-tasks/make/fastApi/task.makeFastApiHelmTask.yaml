tasks:

  makeFastApiHelmTask:
    icon: ⚡
    extend: zrbMakeHelmTask
    dependencies:
      - makeFastApiHelm
      - makeFastApiAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRef: addFastApi