tasks:
 
  makeElasticsearchApp:
    icon: 🍜
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Cassandra'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/elasticsearch/template"
        ]

  addElasticsearch:
    icon: 🍜
    extend: makeContainerAppRunner
    dependencies:
      - makeElasticsearchApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      appIcon: 🍜
      defaultAppDirectory: '{{ .ProjectName }}Cassandra'
      defaultAppPorts: |
        [
          "9200",
          "9300"
        ]