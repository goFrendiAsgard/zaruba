tasks:
 
  makeMysqlApp:
    icon: 🐬
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Mysql'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/mysql/appTemplate"
        ]