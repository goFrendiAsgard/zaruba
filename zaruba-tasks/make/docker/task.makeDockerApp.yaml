tasks:
 
  makeDockerApp:
    icon: 🐳
    extend: makeApp
    inputs:
      - appDirectory
      - appBaseImageName
    configRefs:
      - addDocker
    configs:
      defaultAppBaseImageName: docker.io/bitnami/nginx
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/docker/appTemplate"
        ]