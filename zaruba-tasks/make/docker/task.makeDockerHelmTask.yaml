tasks:

  makeDockerHelmTask:
    icon: 🐳
    extend: zrbMakeHelmTask
    dependencies:
      - makeDockerHelm
      - makeDockerAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRefs:
      - addDocker
