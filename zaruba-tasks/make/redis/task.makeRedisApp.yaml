tasks:
 
  makeRedisApp:
    icon: 🧰
    extend: makeApp
    inputs:
      - appDirectory
    configs:
      defaultAppDirectory: '{{ .ProjectName }}Redis'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/redis/appTemplate"
        ]