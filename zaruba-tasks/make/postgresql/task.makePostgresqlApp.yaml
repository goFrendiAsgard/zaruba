tasks:
 
  makePostgresqlApp:
    icon: 🐘
    extend: makeApp
    inputs:
      - appDirectory
    configs:
      defaultAppDirectory: '{{ .ProjectName }}Posgresql'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/postgresql/appTemplate"
        ]