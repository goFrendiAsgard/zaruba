tasks:

  makeAppKubeDeploymentTask:
    icon: 🚢
    extend: zrbMake
    dependencies:
      - zrbIsProject
      - makeAppKubeDeployment
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppKubeDeploymentTask

