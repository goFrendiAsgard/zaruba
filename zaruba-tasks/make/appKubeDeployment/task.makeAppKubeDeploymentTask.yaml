tasks:

  makeAppKubeDeploymentTask:
    icon: 🚢
    extend: zrbMakeTask
    dependencies:
      - makeAppKubeDeployment
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppKubeDeploymentTask

