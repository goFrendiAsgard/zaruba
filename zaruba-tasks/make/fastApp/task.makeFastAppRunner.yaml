tasks:

  makeFastAppRunner:
    icon: ⚛️
    extend: makePythonAppRunner
    dependencies:
      - makeFastApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeFastAppRunner