tasks:

  makeSimpleGoAppRunner:
    icon: 🐹
    extend: makeGoAppRunner
    dependencies:
      - makeSimpleGoApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeSimpleGoAppRunner