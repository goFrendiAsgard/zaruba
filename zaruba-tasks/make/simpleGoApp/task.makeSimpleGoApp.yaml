tasks:
 
  makeSimpleGoApp:
    icon: 🐹
    extend: makeApp
    inputs:
      - appDirectory
    configRefs:
      - addSimpleGoAppRunner
      - addGoAppRunner
    configs:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/simpleGoApp/appTemplate"
        ]