tasks:

  makeSimpleGoAppHelmTask:
    icon: 🐹
    extend: zrbMakeHelmTask
    dependencies:
      - makeSimpleGoAppHelm
      - makeSimpleGoAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRefs:
      - addSimpleGoAppRunner
      - addGoAppRunner
