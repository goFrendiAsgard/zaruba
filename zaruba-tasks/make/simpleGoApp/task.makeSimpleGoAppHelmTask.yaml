tasks:

  makeSimpleGoAppHelmTask:
    icon: 🐹
    extend: zrbMakeHelmTask
    dependencies:
      - makeSimpleGoAppHelm
      - makeSimpleGoAppRunner
    inputs:
      - appDirectory
      - appName
      - appHelmDirectory
      - appHelmReleaseName
    configRefs:
      - addSimpleGoAppRunner
      - addGoAppRunner
