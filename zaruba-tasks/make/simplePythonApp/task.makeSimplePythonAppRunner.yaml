tasks:

  makeSimplePythonAppRunner:
    icon: 🐍
    extend: makePythonAppRunner
    dependencies:
      - makeSimplePythonApp
    inputs:
      - appDirectory
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRef: makeSimplePythonAppRunner