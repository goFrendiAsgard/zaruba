tasks:

  makeSimplePythonAppRunner:
    icon: 🐍
    extend: makeNativeAppRunner
    dependencies:
      - makeSimplePythonApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    configRefs:
      - makePythonVenvAppRunner
      - makePythonAppRunner
      - addSimplePythonAppRunner
      - addPythonAppRunner