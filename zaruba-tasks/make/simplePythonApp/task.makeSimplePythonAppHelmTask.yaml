tasks:

  makeSimplePythonAppHelmTask:
    icon: 🐍
    extend: zrbMakeHelmTask
    dependencies:
      - makeSimplePythonAppHelm
      - makeSimplePythonAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRefs:
      - addSimplePythonAppRunner
      - addPythonAppRunner
