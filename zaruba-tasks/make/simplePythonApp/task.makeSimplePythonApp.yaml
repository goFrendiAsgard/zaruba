tasks:
 
  makeSimplePythonApp:
    icon: 🐍
    extend: makeApp
    inputs:
      - appDirectory
    configRefs:
      - addSimplePythonAppRunner
      - addPythonAppRunner
    config:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/simplePythonApp/appTemplate"
        ]