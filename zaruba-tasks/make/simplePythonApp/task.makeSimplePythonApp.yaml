tasks:
 
  makeSimplePythonApp:
    icon: 🐍
    extend: makeApp
    inputs:
      - appDirectory
    configRefs:
      - addSimplePythonAppRunner
      - addPythonAppRunner
      - makeSimplePythonApp