tasks:
 
  makeSimplePythonAppHelm:
    icon: 🐍
    extend: makeHelm
    dependencies:
      - makeSimplePythonApp
    inputs:
      - deploymentDirectory
    configRefs:
      - addSimplePythonAppRunner
      - addPythonAppRunner
