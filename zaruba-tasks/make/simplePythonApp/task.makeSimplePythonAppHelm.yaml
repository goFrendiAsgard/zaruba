tasks:
 
  makeSimplePythonAppHelm:
    icon: 🐍
    extend: makeHelm
    dependencies:
      - makeSimplePythonApp
    inputs:
      - appHelmDirectory
    configRefs:
      - addSimplePythonAppRunner
      - addPythonAppRunner
