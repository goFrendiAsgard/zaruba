tasks:

  makeAppKubeDeploymentTaskInPython:
    icon: 🚢
    extend: zrbMake
    dependencies:
      - zrbIsProject
      - makeAppKubeDeploymentInPython
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppKubeDeploymentTask

