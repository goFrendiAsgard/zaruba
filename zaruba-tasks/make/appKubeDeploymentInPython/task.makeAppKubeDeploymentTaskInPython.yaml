tasks:

  makeAppKubeDeploymentTaskInPython:
    icon: 🚢
    extend: zrbMakeTask
    dependencies:
      - makeAppKubeDeploymentInPython
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppKubeDeploymentTask

