apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ztplAppHelmDirectory.fullname" . }}-test-connection"
  labels:
    {{- include "ztplAppHelmDirectory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ztplAppHelmDirectory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
