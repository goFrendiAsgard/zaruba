tasks:

  makeAirflowAppRunner:
    icon: 🎐
    extend: makeDockerComposeAppRunner
    dependencies:
      - makeAirflowApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appImageName
      - appContainerName
    configRef: makeAirflowAppRunner