tasks:
 
  makeAirflowApp:
    icon: 🎐
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Airflow'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/airflow/appTemplate"
        ]