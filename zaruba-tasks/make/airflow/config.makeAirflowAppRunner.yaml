configs:

  makeAirflowAppRunner:
    appIcon: 🎐
    defaultAppDirectory: '{{ .ProjectName }}Airflow'
    defaultAppPorts: |
      [
        "{{ .Template ".GetEnv \\\"AIRFLOW_WEB_SERVER_PORT\\\"" }}"
      ] 