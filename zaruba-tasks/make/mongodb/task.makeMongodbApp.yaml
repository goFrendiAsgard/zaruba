tasks:
 
  makeMongodbApp:
    icon: 🍃
    extend: makeApp
    inputs:
      - appDirectory
    config:
      defaultAppDirectory: '{{ .ProjectName }}Mongodb'
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/mongodb/appTemplate"
        ]