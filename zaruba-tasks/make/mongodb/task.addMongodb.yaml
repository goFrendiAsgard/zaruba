tasks:
 
  makeMongodbApp:
    extend: makeApp
    inputs:
      - appDirectory
    config:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/mongodb/template"
        ]

  addMongodb:
    extend: makeContainerAppRunner
    dependencies:
      - makeMongodbApp
    inputs:
      - appDirectory
      - appDependencies
      - appName
      - appEnvs
      - appPorts
      - appImageName
      - appContainerName
    config:
      defaultAppPorts: |
        [
          "27017"
        ]