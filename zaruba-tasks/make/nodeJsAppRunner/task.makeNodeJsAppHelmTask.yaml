tasks:

  makeNodeJsAppHelmTask:
    icon: 🐸
    extend: zrbMakeHelmTask
    dependencies:
      - makeNodeJsAppHelm
      - makeNodeJsAppRunner
    inputs:
      - appDirectory
      - appName
      - deploymentDirectory
      - deploymentName
    configRef: addNodeJsAppRunner