tasks:

  makeAppDeploymentTaskInPython:
    icon: 🚢
    extend: zrbMakeTask
    dependencies:
      - makeAppDeploymentInPython
    inputs:
      - appDirectory
      - deploymentDirectory
      - deploymentName
    configRef: makeAppDeploymentTask

