tasks:
 
  makeSimpleNodeJsApp:
    icon: 🐸
    extend: makeApp
    inputs:
      - appDirectory
    configRefs:
      - addSimpleNodeJsAppRunner
      - addNodeJsAppRunner
    config:
      templateLocations: |
        [
          "{{ .ZarubaHome }}/zaruba-tasks/make/simpleNodeJsApp/appTemplate"
        ]