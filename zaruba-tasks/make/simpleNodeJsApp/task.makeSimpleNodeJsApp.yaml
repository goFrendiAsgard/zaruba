tasks:
 
  makeSimpleNodeJsApp:
    icon: 🐸
    extend: makeApp
    inputs:
      - appDirectory
    configRefs:
      - addSimpleNodeJsAppRunner
      - addNodeJsAppRunner
      - makeSimpleNodeJsApp