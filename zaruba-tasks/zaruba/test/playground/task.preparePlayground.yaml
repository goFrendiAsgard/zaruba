tasks:

  preparePlayground:
    extend: zrbRunShellScript
    location: ../../../..
    config:
      start: |
        if [ -d "./playground" ]
        then
          chmod 777 -R ./playground
          rm -Rf ./playground
        fi
        mkdir -p ./playground

