tasks:


  makeDocs:
    icon: 🪄
    location: ../../..
    extend: zrbRunShellScript
    dependencies:
      - buildBin
    configs:
      start: |
        . "{{ .GetTaskPath "./bash/makeTaskDocs.sh" }}"
        if [ -f "${HOME}/tocer/tocer.py" ]
        then
          cd docs
          alias zaruba="{{ .ZarubaBin }}"
          python "${HOME}/tocer/tocer.py"
        fi