tasks:

  commitRepo:
    icon: 🔼
    location: ../
    extend: zrbRunShellScript
    dependencies:
      - makeDocs
    config:
      start: |
        git add . -A
        git commit -m "Save changes before pushing"
