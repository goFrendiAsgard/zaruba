tasks:

  setProjectValue:
    icon: 🔗
    description: Set project value.
    extend: zrbRunShellScript
    dependencies:
      - zrbIsProject
    inputs:
      - variableName
      - variableValue
    configRef: setProjectValue