tasks:

  showVersion:
    icon: 🔎
    description: |
      Show zaruba's current version.
    extend: zrbRunShellScript
    configs:
      start: |
        cd "{{ .ZarubaHome }}"
        echo "${_BOLD}${_YELLOW}$(getVersion)${_NORMAL}"