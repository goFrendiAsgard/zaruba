tasks:

  showVersion:
    icon: 🔎
    description: |
      Show zaruba's current version.
    extend: zrbRunShellScript
    configs:
      start: 'echo "${_BOLD}${_YELLOW}$("{{ .ZarubaBin }}" version)${_NORMAL}"'