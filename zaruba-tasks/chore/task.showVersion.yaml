tasks:

  showVersion:
    icon: 🔎
    description: |
      Show zaruba's current version.
    extend: zrbRunCoreScript
    configs:
      start: |
        {{ $d := .Decoration -}}
        cd "{{ .ZarubaHome }}"
        echo "{{ $d.Bold }}{{ $d.Yellow }}$(getVersion){{ $d.Normal }}"