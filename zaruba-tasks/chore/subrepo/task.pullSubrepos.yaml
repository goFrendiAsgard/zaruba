tasks:

  pullSubrepos:
    icon: 🔽
    description: |
      Pull subrepositories.
      ARGUMENTS:
        subrepo::<name>::prefix   : Prefix (directory name) of the subrepo
        subrepo::<name>::url      : Remote url of the subrepo
    extend: zrbRunShellScript
    dependencies:
      - initSubrepos
    configRef: pullSubrepos