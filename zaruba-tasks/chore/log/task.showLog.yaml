tasks:

  showLog:
    icon: 🔎
    description: |
      Show log for all/particular tasks using regex
    extend: zrbRunShellScript
    saveLog: false
    inputs:
      - keyword
    configRef: showLog