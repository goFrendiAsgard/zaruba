tasks:

  clearLog:
    icon: 🔥
    description: |
      Clear log
    extend: zrbRunCoreScript
    configs:
      start: |
        {{ $d := .Decoration -}}
        rm -Rf log.zaruba.csv
        echo "{{ $d.Bold }}{{ $d.Yellow }}Log removed{{ $d.Normal }}"