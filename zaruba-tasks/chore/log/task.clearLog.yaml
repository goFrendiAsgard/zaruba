tasks:

  clearLog:
    icon: 🔥
    description: |
      Clear log
    extend: zrbRunShellScript
    configs:
      start: |
        {{ $d := .Decoration -}}
        rm -Rf log.zaruba.csv
        echo "{{ $d.Bold }}{{ $d.Yellow }}Log removed{{ $d.Normal }}"