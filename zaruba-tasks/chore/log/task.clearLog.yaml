tasks:

  clearLog:
    icon: 🔥
    description: |
      Clear log
    extend: zrbRunShellScript
    configs:
      start: |
        rm -Rf log.zaruba.csv
        echo "${_BOLD}${_YELLOW}Log removed${_NORMAL}"